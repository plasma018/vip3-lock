{
  "name": "semaphore-api",
  "version": "1.0.0",
  "description": "A semaphore API for Distributed locks",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "export NODE_ENV=dev && node index.js",
    "deploy": "export NODE_ENV=production && node index.js",
    "test": "mocha"
  },
  "keywords": [
    "swagger"
  ],
  "license": "Unlicense",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.280.1",
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "supertest": "^3.1.0",
    "swagger-tools": "0.10.1",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "chai": "^4.1.2",
    "should": "^13.2.3"
  }
}
